import {Service} from '../service/users.js';

export class Controller {
    constructor(service) {
        this.service = service;
    }

    singUp(req, resp) {
        // Проверить допустим ли username
    
        // Захешировать password
    
        // Сохранить в бд
    
        // Вернуть id 
    };
}